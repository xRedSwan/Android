<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marti.monapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.jeux.BatNavActivity" />
        <activity android:name=".activities.jeux.DamesActivity" />
        <activity android:name=".activities.jeux.ChevauxActivity" />
        <activity android:name=".activities.jeux.Puissance4Activity" />
        <activity android:name=".activities.jeux.SudokuActivity" />
        <activity android:name=".activities.jeux.EchecsActivity"></activity>
    </application>

</manifest>